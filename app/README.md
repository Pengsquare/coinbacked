# How to build app

1. Create environment and install Python packages

```
python3 -m venv env
source env/bin/activate
pip3 install -r requirements.txt
```

2. Make

```
make 
```

# How to deploy app

Deploy everything in folder `build` to service of your choice.